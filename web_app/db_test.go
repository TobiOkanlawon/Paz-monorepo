package web_app

// import (
// 	"testing"
// 	// "reflect"
// )

// // func TestDB(t *testing.T) {
// // }

// // func TestMemoryStore(t *testing.T) {
// // 	t.Run("initializes the store successfully", func(t *testing.T) {
// // 		_, err := NewSqlStore("memory")

// // 		if err != nil {
// // 			t.Errorf("initializing the store returns an error %q", err)
// // 		}
// // 	})

// // 	t.Run("registers and fetches a user successfully", func(t *testing.T) {
// // 		// store, _ := NewSqlStore("memory")		
		
// // 		// user, _ := store.RegisterUser(FirstName, dummyUser.LastName, dummyUser.EmailAddress, dummyUser.Password)

// // 		// if (dummyUser.FirstName != user.FirstName) && (dummyUser.LastName != user.LastName) && (dummyUser.EmailAddress != user.EmailAddress){
// // 		// 	t.Fatal("doesn't return the correct user")
// // 		// }

// // 		// fetchedUser, _ := store.GetUser(user.ID)

// // 		// if !reflect.DeepEqual(fetchedUser, user) {
// // 		// 	t.Errorf("fetches %#+v instead of %#+v", fetchedUser, user)
// // 		// }
// // 	})
// // }
